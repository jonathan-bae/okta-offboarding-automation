# Pinned to the versions this project was built and tested with.

# Runtime
requests==2.34.2        # HTTP calls to Okta and Slack
python-dotenv==1.2.3    # loads .env into environment variables
flask==3.1.3            # webhook endpoint

# Testing
pytest==9.1.1           # test runner
responses==0.26.3       # mocks HTTP calls made with requests, so tests never hit real APIs
